# 🚀 Setup Rápido - 5 Minutos

**Start em 5 minutos com 3 passos simples**

---

## ✅ Pré-requisitos (Verificar)

- ✅ Python 3.9+ instalado
- ✅ Node.js LTS instalado
- ✅ Pastas `backend/` e `frontend/` presentes
- ✅ Ficheiro `requirements.txt` existe

**Verificar:**

```bash
python --version
node --version
```

... (truncated for archive)
